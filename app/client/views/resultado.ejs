<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Arquivo <%= method %>:</h1>
    <% if (name) { %>
        <p>Nome:
            <%= name %>
        </p>
        <% } else { %>
            <p>Nenhum nome foi enviado.</p>
            <% } %>
                <hr>
                <% if (data) { %>
                    <p>Conteúdo:
                        <%= data %>
                    </p>
                    <% } else { %>
                        <p>Nenhum conteudo foi enviado.</p>
                        <% } %>

</body>

</html>